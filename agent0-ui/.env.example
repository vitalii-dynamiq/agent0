# Agent-0 Environment Variables
# Copy this file to .env.local and fill in the values

# Optional: Analytics
# NEXT_PUBLIC_ANALYTICS_ID=

# Optional: API endpoints (for future backend integration)
# NEXT_PUBLIC_API_URL=https://api.example.com

# Note: This UI currently uses mock data and doesn't require any environment variables.
# Environment variables will be needed when integrating with actual backend services.
